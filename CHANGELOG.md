# 更新日志

## v0.13.0

- 重构首页操作流程为“加载待修补主题 → 添加为自定义图标 → 修补主题”。
- “选择应用”和“管理自定义图标”改为二级弹层，精简首页并优化窄屏布局。
- 自定义图标按主题隔离；切换主题时只显示和修补当前主题的图标。
- “修补主题”统一处理当前主题的全部自定义图标，并继续使用写入前备份、原子替换和写入后哈希校验。
- 自定义图标管理支持单项删除、全选和批量删除。
- 移除独立的“合并主题更新与自定义图标”和“删除并恢复主题”入口。
- 更新主题、桌面及备份相关文案，使按钮含义与实际操作一致。

## v0.12.11

- 在 v0.12.10 已确认有效的目标路径修复基础上，重新启用 Android ARM64 手机端快速生成。
- 快速工具使用与上午成功版本一致的原始 MRC 字节保留和追加覆盖布局。
- 快速工具不可用或生成校验失败时，自动回退到已验证可用的 WebUI 兼容写入链路。
- 保留主题商店目标目录边界保护、写入前后 SHA256 与修改时间审计。

## v0.12.10

- 修复 Shell 函数全局变量污染：ZIP 校验覆盖了商店组件目标路径，导致生成结果被误写入模块 `base-cache`。
- `patch_cache` 使用独立 `TARGET_FILE`，准备基础副本后重新定位并核对目标，防止子函数改变写入路径。
- 增加路径边界保护：目标不在主题商店 `content/icons` 目录时直接拒绝写入。

## v0.12.9

- 以 11:24 最后成功写入时使用的 v0.10.0（提交 `3117a4f`）为精确兼容基线。
- 普通生成的 ZIP 本地头与中央目录字段恢复为该版本布局，移除下午加入的时间字段差异。

## v0.12.8

- 普通图标生成恢复为上午已知可用的 WebUI 兼容写入链路，暂时停用 v0.12.0 引入的手机端快速生成。
- 兼容链路会按包名查找并替换所有实际 drawable 条目，未适配图标才写入首选目录。
- 写入成功日志增加目标绝对路径、写入前后 SHA256 与修改时间，便于确认实际文件确实发生变化。

## v0.12.7

- 手机端工具改为复刻最初可用的兼容写入算法：完整保留 MRC 原始本地条目字节，仅追加自定义图标并重建中央目录。
- 修复自定义图标管理卡片在窄屏设备上超出页面右侧的问题，长包名和操作按钮可安全收缩换行。

## v0.12.6

- 修复 Windows 打包产生反斜杠 ZIP 条目，导致 KernelSU 无法识别 `webroot/index.html`、WebUI 入口消失的问题。
- 打包校验增加 ZIP 原始路径检查，禁止包含反斜杠的安装包交付。

## v0.12.5

- 将项目中面向用户的旧术语统一调整为“配置”，包括界面、日志、错误提示和文档。

## v0.12.4

- “合并主题更新与自定义图标”改为手机端本地快速合并，不再将完整 MRC 和全部 PNG 往返传输 WebUI。
- 本地工具自动识别主题实际 drawable 目录，仅补充新版主题仍未适配的图标。
- 保持“主题新版优先”规则，主题更新已经提供的图标不会被旧自定义图标覆盖。

## v0.12.3

- 新增自定义图标配置管理，可查看、仅删除配置或删除并恢复主题。
- 删除并恢复时从干净基础组件重新应用剩余配置：已适配图标恢复原图，未适配新增图标自动移除。
- 删除的 PNG 先移入回收站；生成失败会自动恢复配置，不直接丢失数据。
- 最终组件写入失败时自动从回收站恢复配置，避免主题和配置状态不一致。
- 写入前保存每个主题组件的干净基础副本，旧版本升级时优先从可用备份建立基础。
- 新增卸载清理脚本；明确卸载模块时删除新旧持久目录及其中的图标、备份、回收站和缓存，覆盖升级仍保留数据。

## v0.12.2

- 将持久数据迁移到与模块 ID 分离的 `/data/adb/hyper_icon_patcher_data`，避免模块升级清理自定义图标。
- 安装升级时自动迁移旧目录中的自定义图标、备份和合并结果。
- 保存新图标配置改为增量合并，同包名图标覆盖更新，未重新选择的旧图标继续保留。

## v0.12.1

- 修复生成时只向全局 drawable 目录添加PNG、没有替换应用原图实际条目的问题。
- 已适配应用会按包名查找并替换所有实际 drawable 密度目录中的同名图标。
- 未适配应用优先写入 `drawable-nodpi-v4` 或 `drawable-nodpi`，兼容 HyperOS 主题图标读取规则。

## v0.12.0

- 新增 Android ARM64 手机端 ZIP 快速生成工具，原始主题组件不再通过 WebUI 完整回传。
- 未修改 ZIP 条目直接复制原始压缩数据，仅替换选择的 PNG 图标。
- 快速生成结果经过 ZIP 和新增图标逐项校验后原子切换。
- 不支持的设备或工具运行失败时自动回退浏览器兼容模式。
- 兼容模式上传初始分块提升到 96KB，并保留 48KB、24KB、12KB 自动降级。

## v0.11.3

- 修复分块上传函数与界面函数重名，导致自定义图标没有实际上传、最终提示“没有可保存的图标配置”的问题。
- 修复参数过长错误识别函数的同名覆盖，恢复上传分块自动降级重试。

## v0.11.2

- 修复部分 Android WebView 无法通过 `createImageBitmap` 解码有效 PNG 的问题，失败时自动改用图片元素解码。
- 解码失败提示增加应用名称、包名和原始文件名，方便定位损坏或不兼容的图片。

## v0.11.1

- 修复生成修改结果时，大型主题数据触发 `/system/bin/sh: Argument list too long` 的问题。
- 上传数据改为小分块传输；遇到设备参数限制时自动清空临时传输并缩小分块重试。
- 更新前端脚本文件名，避免 KernelSU WebUI 继续使用旧缓存。

## v0.11.0

- 修复部分 Android WebView 中 Toast 不可见的问题。
- 更新静态资源文件名，避免 KernelSU WebUI 使用旧缓存。
- 增加全局页面异常、Promise 异常和设备命令超时提示。
- 缓存应用信息，分批读取应用详情，并按可视区域加载应用图标。
- 延迟扫描主题组件，减少与应用列表首屏加载争抢。
- 优化 MRM 主题名称查找路径。
- 增加 ZIP、PNG、组件大小和写入后哈希校验。
- 写入失败时自动回滚，图标配置保存中断时自动恢复。
- 每个主题组件仅保留最近 5 份备份。
- 移除已经废弃的活动主题和 MTZ 后端接口。
