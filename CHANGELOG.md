# 更新日志

## v0.15.1

- 修补完成或无需修补后，主按钮恢复为“修补主题”，不再与下方独立的“打开主题商店”按钮重复。
- 清理修补按钮跳转主题商店的冗余状态与分支，并增加交互契约回归检查。

## v0.15.0

- 移动端“选择应用”和“管理修补组”改为稳定的全屏二级页面，修复输入法弹出时卡顿、截断闪烁及露出底层首页的问题。
- 二级页面接入浏览器历史栈，支持系统返回键和全面屏返回手势；新建修补组返回时会回到管理页面。
- 输入法动画期间暂停图标观察加载，搜索结果则使用浏览器原生懒加载，避免搜索范围外图标不显示。
- 没有历史数据时自动创建一次“默认修补组”；用户主动删除全部修补组后不再重复创建。
- 主操作按钮根据主题、修补组和图标状态动态提示下一步，修补完成后可直接打开主题商店。
- 修补前统一显示主题、修补组、模式和图标数量摘要，覆盖模式额外提示同名图标覆盖风险。
- 应用列表新增“已添加”“本次已选择”“将替换”状态，减少重复选择和误覆盖。
- 抽离统一 `backend-client`，集中处理 KernelSU 命令转义、超时、回调和错误解析。
- 新增统一流程状态模块，集中维护主题、修补组、待添加图标、修补模式和执行状态。
- 移除“重启系统桌面”按钮、前后端操作及相关说明，精简首页通用操作。
- 新增 Windows 双击打包脚本，自动读取版本号、校验模块结构并输出 SHA-256。
- 增加默认组只初始化一次、共享前端模块、动态操作状态和应用图标状态的回归契约。

## v0.14.0

- 引入独立“修补组”：自定义图标不再绑定主题 UUID 或 MRC 文件名，主题重新下载、文件名变化后仍可复用。
- 首页调整为“加载主题 → 选择修补组 → 添加图标 → 选择模式并修补”的四步流程。
- 修补组支持创建、重命名、复制、删除，以及组内图标的单项和批量删除。
- 添加图标只保存到当前修补组，不再提前生成主题；点击“修补主题”时才合并并写入当前主题。
- 新增“仅补全缺失图标”和“覆盖同名图标”两种模式。
- 同一修补组内以应用包名作为唯一键，重复添加直接覆盖，减少冗余。
- 不迁移旧版按主题保存的数据；模块更新仅保留新修补组和必要持久数据。
- “主题商店图标源”调整为“待修补主题”，选中主题后自动读取手机端索引，不再需要手动加载。
- 移除主题的“未修改／已修补／已更新”状态记录及手动刷新按钮；进入 WebUI 及从主题商店返回时自动扫描。
- 统一“管理修补组”和“选择应用”入口按钮的尺寸，并将“添加为自定义图标”移动到待修补图标区域下方。
- 首页标题调整为“选择待修补主题”和“选择修补组”，“添加为自定义图标”简化为“添加到修补组”。
- 修补按钮从点击开始全程锁定，避免索引读取期间重复点击触发并发写入。
- 缺失补全没有可写条目时直接返回“无需修补”，不再生成或覆盖整份 MRC。
- 覆盖同名图标前显示主题、修补组和图标数量确认；修补结果同步显示主题与修补组名称。
- 图标添加成功后显示 Toast 并自动清空待添加列表，失败时主动清理上传暂存。
- 重名主题显示修改时间和文件标识；从主题商店返回 WebUI 后自动重新扫描。
- 修补组图标预览改为进入可视区域后按需加载，减少管理页面首次打开的设备命令数量。
- 单项和批量删除统一使用后端原子批量操作，避免中途失败造成部分删除。
- 四个首页步骤增加轻量状态提示，主题、修补组或模式变化后自动重置修补结果。
- 增加修补组名称字符／字节、重名、组数量、组内图标数量、包名长度、上传 Base64 和源 PNG 尺寸边界校验。
- 模块持久数据限制为 256MB，并在克隆、原子切换、MRC 生成、回滚及主题写入前检查分区可用空间。
- WebUI 启动维护会恢复中断的修补组目录，并清理上传暂存、合并结果、失效回滚文件和 `.hip-new` 孤儿文件。
- 主题标签解析增加按 MRC 大小和修改时间失效的小型缓存，并自动清除已删除主题的缓存。
- 修补组保存和删除对未变化 PNG 使用安全硬链接，避免复制整组图标造成延迟和瞬时双倍占用。
- 手机端 MRC 输出改为流式写入，减少约一整份 MRC 的峰值内存占用，并限制输出不超过 80MB。
- 主题索引只向 WebUI 返回图标目录、条目数量和文件大小，不再传输全部 ZIP 条目名称。
- “生成＋写入”合并为一个带设备端互斥锁的事务，避免命令超时或多个 WebUI 同时操作导致结果串线。

## v0.13.3

- 主题加载改用 `hipzip-arm64 -inspect` 读取中央目录索引，不再向 WebUI Base64 传输完整 MRC。
- 完全移除浏览器端 ZIP 解析、主题重建和完整 MRC 上传回退，仅支持 Android ARM64 手机端快速模式。
- 修复去冗余清理过早删除暂存 PNG，导致手机端快速生成报错 `no staged PNG icons` 并回退的问题。
- 取消长期保存整份主题备份，删除“恢复该主题最近备份”入口。
- 修补时只创建一份临时回滚文件；写入或哈希校验失败时立即恢复，成功后立即删除。
- 模块更新时自动清理旧版 `cache-backups`，进一步降低持久化空间占用。
- 需要撤销已成功修补的主题时，改为从主题商店重新下载原主题。

## v0.13.2

- 移除整份 MRC 的 `patched-cache`、`base-cache` 和 WebUI IndexedDB 副本，显著降低空间占用。
- 加载、生成和修补完成后主动清理 Base64、临时 ZIP 与上传暂存文件。
- 删除自定义图标时直接移除，不再写入长期累积的模块回收站。
- 模块更新保留各主题自定义图标、状态和最近两份备份，并清理所有可重建缓存。
- 旧版全局自定义图标在首次加载主题时自动迁移到主题专属目录。
- 卸载模块时清除新旧持久化数据目录及主题目录中的模块临时文件。

## v0.13.1

- 首页进一步调整为“选择并加载主题 → 准备待修补图标 → 执行操作”的三步式布局。
- “添加为自定义图标”增加主题加载前置校验，防止图标脱离主题保存。
- 待修补图标最多显示 5 行，更多内容在列表内滚动，并增加选择计数与一键清空。
- 待修补区显示当前主题标签，避免切换主题后误操作。
- 低频的主题备份恢复移入“更多操作”，运行日志默认折叠且错误时自动展开。
- 修补成功后显示独立结果卡片，不再要求用户仅通过日志确认结果。
- Toast 提示展示 3 秒后自动消失，每个主题最多保留最近 2 份备份。
- 同一主题下以应用包名作为自定义图标唯一键，重复添加时直接覆盖，并在保存成功后清理上传暂存副本。

## v0.13.0

- 重构首页操作流程为“加载待修补主题 → 添加为自定义图标 → 修补主题”。
- “选择应用”和“管理自定义图标”改为二级弹层，精简首页并优化窄屏布局。
- 自定义图标按主题隔离；切换主题时只显示和修补当前主题的图标。
- “修补主题”统一处理当前主题的全部自定义图标，并继续使用写入前备份、原子替换和写入后哈希校验。
- 自定义图标管理支持单项删除、全选和批量删除。
- 移除独立的“合并主题更新与自定义图标”和“删除并恢复主题”入口。
- 更新主题、桌面及备份相关文案，使按钮含义与实际操作一致。

## v0.12.11

- 在 v0.12.10 已确认有效的目标路径修复基础上，重新启用 Android ARM64 手机端快速生成。
- 快速工具使用与上午成功版本一致的原始 MRC 字节保留和追加覆盖布局。
- 快速工具不可用或生成校验失败时，自动回退到已验证可用的 WebUI 兼容写入链路。
- 保留主题商店目标目录边界保护、写入前后 SHA256 与修改时间审计。

## v0.12.10

- 修复 Shell 函数全局变量污染：ZIP 校验覆盖了商店组件目标路径，导致生成结果被误写入模块 `base-cache`。
- `patch_cache` 使用独立 `TARGET_FILE`，准备基础副本后重新定位并核对目标，防止子函数改变写入路径。
- 增加路径边界保护：目标不在主题商店 `content/icons` 目录时直接拒绝写入。

## v0.12.9

- 以 11:24 最后成功写入时使用的 v0.10.0（提交 `3117a4f`）为精确兼容基线。
- 普通生成的 ZIP 本地头与中央目录字段恢复为该版本布局，移除下午加入的时间字段差异。

## v0.12.8

- 普通图标生成恢复为上午已知可用的 WebUI 兼容写入链路，暂时停用 v0.12.0 引入的手机端快速生成。
- 兼容链路会按包名查找并替换所有实际 drawable 条目，未适配图标才写入首选目录。
- 写入成功日志增加目标绝对路径、写入前后 SHA256 与修改时间，便于确认实际文件确实发生变化。

## v0.12.7

- 手机端工具改为复刻最初可用的兼容写入算法：完整保留 MRC 原始本地条目字节，仅追加自定义图标并重建中央目录。
- 修复自定义图标管理卡片在窄屏设备上超出页面右侧的问题，长包名和操作按钮可安全收缩换行。

## v0.12.6

- 修复 Windows 打包产生反斜杠 ZIP 条目，导致 KernelSU 无法识别 `webroot/index.html`、WebUI 入口消失的问题。
- 打包校验增加 ZIP 原始路径检查，禁止包含反斜杠的安装包交付。

## v0.12.5

- 将项目中面向用户的旧术语统一调整为“配置”，包括界面、日志、错误提示和文档。

## v0.12.4

- “合并主题更新与自定义图标”改为手机端本地快速合并，不再将完整 MRC 和全部 PNG 往返传输 WebUI。
- 本地工具自动识别主题实际 drawable 目录，仅补充新版主题仍未适配的图标。
- 保持“主题新版优先”规则，主题更新已经提供的图标不会被旧自定义图标覆盖。

## v0.12.3

- 新增自定义图标配置管理，可查看、仅删除配置或删除并恢复主题。
- 删除并恢复时从干净基础组件重新应用剩余配置：已适配图标恢复原图，未适配新增图标自动移除。
- 删除的 PNG 先移入回收站；生成失败会自动恢复配置，不直接丢失数据。
- 最终组件写入失败时自动从回收站恢复配置，避免主题和配置状态不一致。
- 写入前保存每个主题组件的干净基础副本，旧版本升级时优先从可用备份建立基础。
- 新增卸载清理脚本；明确卸载模块时删除新旧持久目录及其中的图标、备份、回收站和缓存，覆盖升级仍保留数据。

## v0.12.2

- 将持久数据迁移到与模块 ID 分离的 `/data/adb/hyper_icon_patcher_data`，避免模块升级清理自定义图标。
- 安装升级时自动迁移旧目录中的自定义图标、备份和合并结果。
- 保存新图标配置改为增量合并，同包名图标覆盖更新，未重新选择的旧图标继续保留。

## v0.12.1

- 修复生成时只向全局 drawable 目录添加PNG、没有替换应用原图实际条目的问题。
- 已适配应用会按包名查找并替换所有实际 drawable 密度目录中的同名图标。
- 未适配应用优先写入 `drawable-nodpi-v4` 或 `drawable-nodpi`，兼容 HyperOS 主题图标读取规则。

## v0.12.0

- 新增 Android ARM64 手机端 ZIP 快速生成工具，原始主题组件不再通过 WebUI 完整回传。
- 未修改 ZIP 条目直接复制原始压缩数据，仅替换选择的 PNG 图标。
- 快速生成结果经过 ZIP 和新增图标逐项校验后原子切换。
- 不支持的设备或工具运行失败时自动回退浏览器兼容模式。
- 兼容模式上传初始分块提升到 96KB，并保留 48KB、24KB、12KB 自动降级。

## v0.11.3

- 修复分块上传函数与界面函数重名，导致自定义图标没有实际上传、最终提示“没有可保存的图标配置”的问题。
- 修复参数过长错误识别函数的同名覆盖，恢复上传分块自动降级重试。

## v0.11.2

- 修复部分 Android WebView 无法通过 `createImageBitmap` 解码有效 PNG 的问题，失败时自动改用图片元素解码。
- 解码失败提示增加应用名称、包名和原始文件名，方便定位损坏或不兼容的图片。

## v0.11.1

- 修复生成修改结果时，大型主题数据触发 `/system/bin/sh: Argument list too long` 的问题。
- 上传数据改为小分块传输；遇到设备参数限制时自动清空临时传输并缩小分块重试。
- 更新前端脚本文件名，避免 KernelSU WebUI 继续使用旧缓存。

## v0.11.0

- 修复部分 Android WebView 中 Toast 不可见的问题。
- 更新静态资源文件名，避免 KernelSU WebUI 使用旧缓存。
- 增加全局页面异常、Promise 异常和设备命令超时提示。
- 缓存应用信息，分批读取应用详情，并按可视区域加载应用图标。
- 延迟扫描主题组件，减少与应用列表首屏加载争抢。
- 优化 MRM 主题名称查找路径。
- 增加 ZIP、PNG、组件大小和写入后哈希校验。
- 写入失败时自动回滚，图标配置保存中断时自动恢复。
- 每个主题组件仅保留最近 5 份备份。
- 移除已经废弃的活动主题和 MTZ 后端接口。
